# Place local ad image files here (PNG/JPG/WebP).
# Reference them in admin as: ad add banner|id|/ads/my-banner.png|https://click.example|640|100
# Or set MOBILE_API_PUBLIC_BASE so the API returns absolute URLs.
